# 安全策略

## 这个项目会处理什么

影书会把**麦克风捕获的全部音频**上传到你自己的服务器，并转写成文字。
请按"这些数据等同于你全部的现实对话"来对待它。

## 部署时必须做的事

| 项 | 为什么 |
|---|---|
| **设置 `SS_TOKEN`** | 未设置时服务端只生成一个进程内临时 token。这能挡住扫描器，但**不构成安全边界** |
| **不要直接暴露到公网** | MVP 阶段传输是明文 HTTP。用反向代理 + TLS、SSH 隧道或 VPN |
| **考虑 `SS_KEEP_AUDIO=false`** | 蒸馏后自动删除音频，只留文字 |
| **限制 `/data` 访问权限** | 里面是完整对话记录 |
| **定期 `shadowscribe prune`** | 清理超出保留期的音频 |

## 已知设计取舍（不是漏洞）

- **无端侧加密**：MVP 阶段有意为之。服务端能看到全部明文。
- **Token 是静态共享密钥**：没有过期、没有轮换、没有多用户。单用户工具够用。
- **无速率限制**：带 token 的请求不限速。
- **`/docs`（Swagger UI）默认开启**：方便调试。生产部署建议在反向代理层关掉。

## 报告漏洞

**请不要开公开 issue。**

使用 GitHub 的 [私密漏洞报告](https://github.com/yw1103/ShadowScribe/security/advisories/new)，
或直接联系仓库所有者。

请包含：影响、复现步骤、受影响的版本或 commit。会在 7 天内回复。

## 不在范围内

- 由使用者未设置 `SS_TOKEN` 或直接公网暴露明文端口导致的问题
- 使用者违反所在地录音法规的情形
- 社会工程学
